# =============================================================================
# SLIDE R vs Python Comparison Configuration
# =============================================================================
# Copy this file and modify for your experiment
#
# Usage:
#   sbatch run_comparison.sh my_config.yaml
#   bash run_comparison.sh my_config.yaml
# =============================================================================

# -----------------------------------------------------------------------------
# Data Paths (REQUIRED)
# -----------------------------------------------------------------------------
x_path: /ix/djishnu/Aaron/1_general_use/SLIDE/Data_Scripts/SSc/UnTx/X.csv
y_path: /ix/djishnu/Aaron/1_general_use/SLIDE/Data_Scripts/SSc/UnTx/Y.csv

# -----------------------------------------------------------------------------
# Output Settings
# -----------------------------------------------------------------------------
# Output subdirectory name
tag: SSc_comparison

# Base output directory (use absolute path to avoid issues)
out_path: /ix/djishnu/Aaron/1_general_use/SLIDE_py/comparison/outputs

# -----------------------------------------------------------------------------
# SLIDE Parameters (same for R and Python)
# -----------------------------------------------------------------------------
# Delta threshold for pure variable detection
delta: 0.1

# Lambda regularization parameter
lambda: 0.5

# Specificity threshold for knockoffs
spec: 0.1

# False discovery rate threshold
fdr: 0.1

# Number of SLIDE knockoff iterations
niter: 500

# FDR threshold for correlation matrix
thresh_fdr: 0.2

# -----------------------------------------------------------------------------
# Environment (optional - uses defaults if not specified)
# -----------------------------------------------------------------------------
# python_env: /ix3/djishnu/AaronR/8_build/.conda/envs/loveslide_env/bin/python
